import React from 'react';

import ReposListContainer from './ReposListContainer';

const Layout = () => {
    return (
        <>
            <ReposListContainer />
        </>
    );
};

export default Layout;
